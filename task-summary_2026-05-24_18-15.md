# 任务摘要 - 2026-05-24 18:15

## 目标
修复中医管理和急救联动页面的"查看"按钮无法使用问题，以及英文编码显示为中文的问题

## 关键修复

### 1. 查看按钮功能修复
- **TcmList.vue**: `handleView()` 从 `console.log` 改为设置 `currentRecord` 并打开 `dialogVisible`
- **EmergencyList.vue**: 同上修复

### 2. 中医管理列表字段显示修复
- **证型列**: 移除英文key映射（`qi_deficiency`→`气虚质`），直接显示后端返回的中文值（如"气阴两虚证"）
- **中医病名列**: prop 从 `disease_code`（ICD编码，如I20）改为 `tcm_disease`（中文，如"消渴"）

### 3. 急救联动列表字段显示修复
- **急救类型**: 补充大写英文映射（`SEVERE_DYSPNEA`→严重呼吸困难、`STROKE_SUSPECT`→疑似脑卒中、`HYPERTENSIVE_CRISIS`→高血压危象等8种）
- **状态码**: 补充大写映射（`ARRIVED`→已到达、`PROCESSING`→处理中、`IN_TRANSIT`→转运中等）

## 修改文件
- `frontend/src/views/TcmList.vue` - 查看按钮+字段显示
- `frontend/src/views/EmergencyList.vue` - 查看按钮+类型/状态映射

## 验证状态
- Vite dev server 运行正常 (localhost:3000)
- 后端 uvicorn 运行正常 (localhost:8000)
- 前端热更新自动生效

# API 端点和数据库修复 - 2026-05-31

## 服务器状态
- **IP**: 47.93.98.197
- **Web**: http://47.93.98.197 ✅ 正常
- **SSH**: ❌ 密码被拒绝 (Permission denied) — 需通过阿里云 VNC Workbench 恢复

## API 端点状态（最新）

| 端点 | 状态 | 说明 |
|------|------|------|
| auth/login | ✅ 200 | admin/admin123 |
| dashboard/stats | ✅ 200 | 数据展示正常 |
| dashboard/kpi | ✅ 200 | KPI 考核指标 |
| followups | ✅ 200 | 随访列表 |
| alerts | ✅ 200 | 预警列表 |
| prescription-reviews | ✅ 200 | 处方审核 |
| health-education/* | ✅ 200 | 健康宣教 |
| admin/org-tree | ✅ 200 | 机构树 |
| patients | ⚠️ 200 但 total=0 | DB 数据被清空 |
| scoring/list | ❌ 404 | 未实现 |
| audit-logs | ❌ 404 | 未实现 |
| county-township | ❌ 404 | 未实现 |

## 数据库状态

**问题**：所有表清空（之前的 seed_v5.py 执行了 DELETE 但 INSERT 因列数不匹配失败）

**数据**：patient=0, followup_record=0, alert_record=0, patient_medication=0
**Schema**：31 表完整

## 根因分析

1. seed_v5.py 先成功 DELETE 所有表
2. 患者 INSERT 成功（列数正确：17 列）
3. 随访 INSERT 失败：26 values for 25 columns（实际表有 37 列，而非 25）
4. 后续 INSERT 全未执行

## 修复方案

已准备好 `fix_server_seed.py`：
```
/Users/shayuen/.qclaw/workspace/slow_disease_system/fix_server_seed.py
```

需要用户通过**阿里云 VNC Workbench** 登录 VPS 后执行：

```bash
# 1. 先恢复 SSH（可选）
systemctl start ssh
systemctl enable ssh

# 2. 重启应用服务确保最新代码运行
cd /opt/lingshui
git pull origin main
systemctl restart lingshui-api

# 3. 将 fix_server_seed.py 传到服务器并执行
cd /opt/lingshui/backend
# （用 vim 或其他方式粘贴脚本内容到服务器）
venv/bin/python3 fix_server_seed.py

# 4. 验证
curl -s "http://127.0.0.1:8000/api/v1/patients" \
  -H "Authorization: Bearer $(curl -s -X POST http://127.0.0.1:8000/api/v1/auth/login \
  -H 'Content-Type: application/json' -d '{"username":"admin","password":"admin123"}' | \
  python3 -c 'import sys,json;print(json.load(sys.stdin)["access_token"])')" | \
  python3 -c 'import sys,json;d=json.load(sys.stdin);print("patients:",d.get("total"))'
```

## 待解决 — 长期

1. **SSH 不稳定**：VPS 22 端口频繁断开，需排查（可能是阿里云安全组或资源限制）
2. **P0 缺口**：scoring/list、audit-logs、county-township 路由需实现
3. **P1 优化**：处方 AI 推荐（需 Ollama）、Vite chunk 优化
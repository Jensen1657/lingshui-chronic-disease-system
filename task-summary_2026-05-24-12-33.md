# 转诊列表修复 - 2026-05-24 12:33

## 目标
修复转诊列表页面三个问题：
1. 缺少患者姓名列
2. 申请机构显示编码（如46012305）而非名称
3. 接收机构显示编码而非名称

## 修改文件

### 后端
1. **backend/app/api/referral.py**
   - 添加 `ORG_NAME_MAP` 机构编码→名称映射字典（13个陵水县医疗机构）
   - `list_referrals` 接口：去掉 `response_model=PaginatedReferralResponse`（schema会过滤掉额外字段）
   - 查询后批量获取患者姓名（通过 encryption_service 解密 name_enc）
   - 返回 dict 时补充 `patient_name`、`apply_org_name`、`receive_org_name` 字段

2. **backend/app/schemas/referral.py**
   - `ReferralResponse` 添加可选展示字段：`patient_name`、`apply_org_name`、`receive_org_name`

### 前端
3. **frontend/src/views/ReferralList.vue**
   - 表格添加 `patient_name` 列（"患者姓名"）
   - `apply_org_code` → `apply_org_name`
   - `receive_org_code` → `receive_org_name`

## 验证结果
- API 返回：`ref008 | 张三 (p_0001) | 三才镇卫生院 → 陵水县人民医院` ✅
- 旧 seed 数据（P001/P002）机构编码 469028 不在映射表中，显示原始编码（可接受）

## 注意事项
- 少量旧转诊记录的 apply_org_code 为非标准编码（如469028），不在ORG_NAME_MAP中
- `response_model` 会静默过滤 schema 中不存在的字段，这是之前返回值被覆盖的根因

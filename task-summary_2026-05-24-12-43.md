# 转诊列表二次修复 - 2026-05-24 12:43

## 问题
用户截图显示：
1. P001/P002 患者姓名显示为编号（非真实姓名）
2. 部分接收机构仍显示编码（46012301等）而非名称

## 修复

### 1. 前端 receive_org_name 未生效
**文件**: `frontend/src/views/ReferralList.vue`
**原因**: 接收机构列模板仍写的是 `{{ row.receive_org_code || '-' }}`
**修复**: 改为 `{{ row.receive_org_name || '-' }}`

### 2. 脏数据修复
**数据**: ref001/ref002 两条转诊记录引用不存在的患者 P001/P002，且机构编码为非标准值 469028
**修复**: SQL 直接更新 `apply_org_code='46012301'`(陵水县人民医院), `receive_org_code='46012303'`(椰林镇卫生院)

### 3. P001/P02 姓名显示
这两条转诊引用的患者在 patient 表中不存在，后端代码已有 fallback：`patient_names.get(r.patient_id, r.patient_id)`，会显示 patient_id 作为姓名。这是预期行为。

## 验证
- API 返回 apply_org_name/receive_org_name 正确 ✅
- 前端模板绑定 receive_org_name ✅
- 脏数据机构编码已修正 ✅

# 任务摘要 - 2026-05-24 16:45~16:50

## 目标
修复用户报告的慢病管理系统问题：截图显示 localhost:3000 本地版本 Dashboard 全为0 + "服务器错误"

## 排查过程
1. **后端 ImportError**: `cannot import name 'encryption_service' from 'app.services.encryption_service'`
   - 文件定义了 `EncryptionService` 类和 `get_encryption_service()` 函数，但缺少模块级单例变量
   - 修复：在 encryption_service.py 末尾添加 `encryption_service = get_encryption_service()`

2. **Dashboard KPI OperationalError**: `no such function: to_char`
   - dashboard.py 使用了 PostgreSQL 专属函数（to_char, CURRENT_DATE - INTERVAL），本地开发环境是 SQLite
   - 修复：to_char → func.strftime()，CURRENT_DATE - INTERVAL → date('now', '-N days')

3. **NameError: name 'strftime' is not defined**
   - SQLAlchemy 中需用 func.strftime 而非裸 strftime
   - 修复：改为 func.strftime()

## 修复文件
- `backend/app/services/encryption_service.py` — 添加模块级单例
- `backend/app/api/dashboard.py` — 3处PostgreSQL语法改为SQLite兼容

## 验证结果
- ✅ 登录接口正常（admin/admin123）
- ✅ KPI 接口返回正常数据（64患者、174随访等）
- ✅ 患者列表返回53条记录，姓名/机构明文显示正确

## 当前状态
- 后端 uvicorn PID 22265 运行在 localhost:8000
- 前端 Vite 运行在 localhost:3000
- Docker backend/frontend 容器已停止
- Docker postgres/redis 容器运行中

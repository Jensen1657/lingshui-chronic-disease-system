# FastAPI 核心框架
fastapi==0.115.0
uvicorn[standard]==0.30.0

# 数据库相关
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0  # PostgreSQL 异步驱动（生产环境使用）
aiosqlite==0.19.0  # SQLite 异步驱动（开发环境使用）
alembic==1.13.0  # 数据库迁移（可选）

# 数据验证与配置
pydantic==2.9.0
pydantic-settings==2.5.0
email-validator==2.2.0

# 认证与安全
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9  # 支持表单数据
cryptography==43.0.0  # 数据加密（PII 字段加密）

# 日志与工具
loguru==0.7.2
python-dotenv==1.0.0

# 可选：Redis 缓存
# redis==5.0.0
# hiredis==3.0.0

# 可选：数据导出
# openpyxl==3.1.0  # Excel 导出
# reportlab==4.2.0  # PDF 导出
gunicorn==22.0.0  # WSGI 服务器（生产环境）
psycopg2-binary==2.9.9  # PostgreSQL 同步驱动（Alembic 迁移需要）
